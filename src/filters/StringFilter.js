/**
 * Created by leiyao on 2017/5/18.
 */

export const upper = (txt)=> txt.toUpperCase();
export const lower = (txt)=> txt.toLowerCase();
export const capitalize = (txt)=> text[0].toUpperCase() + text.slice(1);
